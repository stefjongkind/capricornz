class Page
  attr_accessor :page, :section, :title, :stylesheet, :template
  
  def initialize(options)
    @name = options[:page]
    @title = options[:title]
    @section = options[:section]
    @stylesheet = options[:stylesheet]
    @template = options[:template]
  end
  
end